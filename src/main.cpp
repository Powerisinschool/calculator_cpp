// #include <iostream>
// #include "parser/parser.h"

// void printOp(BinaryOp op)
// {
//     switch (op)
//     {
//     case BinaryOp::PLUS:
//         std::cout << '+' << std::endl;
//         break;
//     case BinaryOp::MINUS:
//         std::cout << '-' << std::endl;
//         break;
//     case BinaryOp::MULTIPLY:
//         std::cout << "* | x" << std::endl;
//         break;
//     default:
//         std::cout << "???" << std::endl;
//     }
// }

int main()
{
    // Parser p("1 + 2 * 3");
    // auto expr = p.parseExpression();
    // auto plus = dynamic_cast<BinaryExpr *>(expr.get());
    // printOp(plus->op);
    // auto mult = dynamic_cast<BinaryExpr *>(plus->right.get());
    // printOp(mult->op);

    return 0;
}
